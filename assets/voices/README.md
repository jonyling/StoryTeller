# Built-in narrator reference clips

These WAVs (~12–16 s) are speaker references for local XTTS cloning:

| File | Narrator |
| :--- | :--- |
| `warm.wav` | Warm grandparent |
| `bright.wav` | Bright & bouncy |
| `gentle.wav` | Gentle bedtime |

They are synthetic demo samples (Edge TTS), for class/demo use only — not
commercial voice products. Re-generate with `scripts/generate_builtin_voices.py`
if needed.
